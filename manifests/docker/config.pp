#
#
# == Class: artifactory::docker::config
#
# Configures artifactory when using a docker installation.
#
#
class artifactory::docker::config {

  # does not do anything. But must be present otherwise there's an error.

}
